# Flask
FLASK_APP=run:app
FLASK_ENV=development
SECRET_KEY=your-secret-key-change-in-production

# PostgreSQL (or set DATABASE_URL directly)
# DATABASE_URL=postgresql://user:password@localhost:5432/fitness_app
PGUSER=postgres
PGPASSWORD=postgres
PGHOST=localhost
PGPORT=5432
PGDATABASE=fitness_app

# JWT
JWT_SECRET_KEY=your-jwt-secret-change-in-production

# Optional: upload directory for meal images (defaults to backend/uploads)
# UPLOAD_FOLDER=./uploads
